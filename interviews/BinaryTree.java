package interviews;

public class BinaryTree {
    BTNode root;

    public BinaryTree(int value) {
        root = new BTNode(value);
    }

    public BinaryTree() {
        root = null;
    }

    // create an initial tree maybe
}
